// Used for parsing banner pages
var bancourse = {};

// this structure is not satisfactory. Should refactor such that
// banner and open course list parsing / scraping code have amore clear relationship

(function(lib){

	lib.parse = function(raw_object){
		var parsed = {
			// TODO move parsing code here from the mess in banner.js
		};
		return parsed;
	};

})(bancourse);

exports = module.exports = bancourse;
